import React from 'react';
import { Content } from '@/pages/content';

// Temporarily redirect to content page until dedicated generate page is built
export function Generate() {
  return <Content />;
}